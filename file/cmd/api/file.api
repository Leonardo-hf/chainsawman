type UploadReply {
	ID   string `json:"id"`
	Size int64  `json:"size"`
}

type FetchReq {
	ID string `path:"id"`
}

@server(
	prefix: /api/file
)
service file {
	@doc "上传文件"
	@handler upload
	post /upload returns (UploadReply)
	
	@doc "下载文件"
	@handler fetch
	get /fetch/:id (FetchReq)
}