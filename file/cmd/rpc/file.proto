syntax = "proto3";

package file;

option go_package = "./rpc";

message IDReq{
  string id = 1;
}

message FileReply{
  string name = 1;
  int64 size = 2;
  bytes data = 3;
}

service file {
  rpc fetchFile(IDReq) returns(FileReply);
}